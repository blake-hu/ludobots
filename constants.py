import numpy as np

# For simulation
duration = 1  # Simulation duration in seconds
resolution = 1000
steps = duration * resolution
gravity = -9.8

amplitude = np.pi/4
frequency = 0.5
offset = np.pi/4
maxForce = 50

save_dir = "./data/"
