import numpy as np

# For simulation
duration = 3  # Simulation duration in seconds
steps = duration * 100
gravity = -9.8

amplitude = np.pi/4
frequency = 0.5
offset = np.pi/4
maxForce = 50

save_dir = "./data/"
